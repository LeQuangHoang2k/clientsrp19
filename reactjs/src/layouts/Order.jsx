import React from 'react';
import PropTypes from 'prop-types';

Order.propTypes = {
    
};

function Order(props) {
    return (
        <div>
            
        </div>
    );
}

export default Order;